---
import videoPresentation from '../assets/videos/adicionando-cilios.mp4';
---

<main class="container pb-12 sm:pb-16">
  <div
    class="relative px-8 pt-96 bg-gradient-to-t from-blessed-black text-white rounded-2xl overflow-hidden max-sm:px-4 max-sm:pt-12"
  >
    <video
      id="video"
      class="absolute inset-0 size-full -z-10 object-cover"
      muted
      playsinline
      loop
      width="1280"
      height="720"
      src={videoPresentation}
      autoplay></video>

    <div
      class="bg-blessed-black/60 rounded-xl flex flex-col items-start gap-2 p-4 mb-8 sm:gap-8 sm:items-center sm:py-1 sm:pl-4 sm:pr-1 sm:bg-blessed-black sm:inline-flex sm:flex-row sm:rounded-full"
    >
      <p class="py-2">Realce sua beleza, descubra sua melhor versão.</p>
      <a
        href="#"
        class="px-4 py-2 rounded-full bg-blessed-primary text-blessed-white inline-flex whitespace-nowrap flex-shrink-0 hover:bg-blessed-white hover:text-blessed-primary transition-colors"
      >
        Agendar agora
      </a>
    </div>

    <h1
      class="font-serif text-balance max-w-screen-lg text-2xl sm:text-3xl lg:text-5xl/normal pb-8"
    >
      Venha sentir-se única, valorizada e bem cuidada
    </h1>
  </div>
</main>
